<style>
	body {
    //margin: 40px 10px;
		padding: 0;
		font-family: "Lucida Grande",Helvetica,Arial,Verdana,sans-serif;
		font-size: 14px;
	}
	#calendar {
		width: 330px;
        height:150px;
       margin: 0 auto;
    background-color:#ffffff;
	}
</style>
<div id='calendar' Class='fc fc-ltr ui-widget'></div>
<div id="eventContent" title="Event Details" style="display:none;margin-right:0px;">
   Start: <span id="startTime"></span><br>
    <span id="endTime"></span><br><br>
    <p id="eventInfo"></p>
    </div>
<div class='elx-datahtml-script'>
var parent = $("#"+viewId);
var data = parent.data("data");
var today = new Date();
var dd = today.getDate();
var mm = today.getMonth()+1; 
var dateArrStart=[];
var recurArr=[];
var descriptionArrrepeat =[];
var eventsArr =[];
var yyyy = today.getFullYear();
var timeArr =[];

  if(data!=null){
        var viewHolder = elx.host.getViewHolder(viewId);
  if(viewHolder){
  var v = new elx.CalendarView(view, data);
    viewHolder.setBaseView(v);
    viewHolder.resize();
  }
  }
</div>